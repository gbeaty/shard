package shard

import boopickle._

object Picklers {

  /*implicit def uriPickler(uri: java.net.URI) {
    Pickler.string.
  }*/

  // def test(row: RNel[Double,RNel[Int,RNil.type]]) = Pickle.intoBytes(row)
}